name: "Test Dispatch event"
on:
  repository_dispatch:
    types: ["category-tag-created"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: echo
        run: |
          echo "we good!"
